---
import Layout from "../../layouts/Layout.astro";
import { getLaunchesByID } from "../../lib/api";
export const prerender = false;
const { id } = Astro.params;
let launch;
if (id) launch = await getLaunchesByID({ id });
---

<Layout title=`Launch ${id}`>
  {launch.id}
</Layout>
